<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>getCRUD</title>
    <style>
        table,
        th,
        td {
            border: 1px solid black;
            text-align: center;
        }

        .btn_edit,
        .btn_delete {
            display: inline-block;
            background-color: orange;
            border-radius: 3px;
            text-decoration: none;
            width: 60px;
            height: 18px;
        }

        .btn_delete {
            background-color: red;
        }
    </style>
</head>

<body>
    <h2>Users table</h2>

    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Phone</th>
            <th>Action</th>
        </tr>
        <% for(let i=0; i < dataUsers.length; i++) { %>
            <tr>
                <td>
                    <%=dataUsers[i].email%>
                </td>
                <td>
                    <%=dataUsers[i].firstName%>
                </td>
                <td>
                    <%=dataUsers[i].lastName%>
                </td>
                <td>
                    <%=dataUsers[i].address%>
                </td>
                <td>
                    <%=dataUsers[i].phoneNumber%>
                </td>
                <td>
                    <a href="/edit-crud?id=<%=dataUsers[i].id%>" class="btn_edit">Edit</a>
                    <a href="" class="btn_delete">Delete</a>
                </td>
            </tr>
            <%}%>
    </table>

    <p>To understand the example better, we have added borders to the table.</p>

</body>

</html>